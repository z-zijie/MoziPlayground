已生成并校验通过：

- [PRD 文件](/Users/eureka/Workspace/MoziPlayground/docs/mozi/abs/prd.md)

要点：
- 覆盖了 `Abs` 的逐元素语义、任意维度支持、空 Tensor/标量 Tensor、`NaN`/`Inf`/`-0.0` 等边界行为
- 面向 `dav-2201`，但未虚构数值型性能指标
- 校验器已通过

如果你要，我可以继续把这个 PRD 转成 SPEC 草案。